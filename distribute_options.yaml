output: dist/

releases:
  - name: dev
    jobs:
      - name: release-linux-arm64
        package:
          platform: linux
          target: appimage
          build_args:
            target-platform: linux-arm64
          publish_to: none

  - name: stable
    jobs:
      - name: release-linux-arm64
        package:
          platform: linux
          target: appimage
          build_args:
            target-platform: linux-arm64
          publish_to: none